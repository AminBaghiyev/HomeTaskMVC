@using Inance.DTOs.MasterDTOs
@model IEnumerable<GetMasterDto>

<!-- Begin Page Content -->
<div class="container-fluid">
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Masters</h6>
        </div>
        <div class="d-flex justify-content-end px-3">
            <a class="btn btn-primary mt-3" style="width: 160px" asp-controller="Master" asp-action="Create">+ Add Master</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Fullname</th>
                            <th>Phone Number</th>
                            <th>Experience Year</th>
                            <th>Active</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (GetMasterDto master in Model)
                        {
                            <tr>
                                <td>@master.Id</td>
                                <td>@master.Fullname</td>
                                <td>@master.PhoneNumber</td>
                                <td>@master.ExperienceYear</td>
                                <td>@master.IsActive</td>
                                <td>
                                    <a class="btn btn-primary" asp-controller="Master" asp-action="Update" asp-route-id="@master.Id">Edit</a>
                                    <a class="btn btn-info" asp-controller="Master" asp-action="Details" asp-route-id="@master.Id">Details</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- /.container-fluid -->
<!-- Page level plugins -->
<script src="~/admin/vendor/datatables/jquery.dataTables.min.js" defer></script>
<script src="~/admin/vendor/datatables/dataTables.bootstrap4.min.js" defer></script>

<!-- Page level custom scripts -->
<script src="~/admin/js/demo/datatables-demo.js" defer></script>