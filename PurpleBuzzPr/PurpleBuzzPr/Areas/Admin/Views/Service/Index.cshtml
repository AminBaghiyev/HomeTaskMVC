@model IEnumerable<Service>

<!-- Begin Page Content -->
<div class="container-fluid">
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Services</h6>
        </div>
        <div class="d-flex justify-content-end px-3">
            <a class="btn btn-primary mt-3" style="width: 160px" asp-area="Admin" asp-controller="Service" asp-action="Create">+ Add Service</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Title</th>
                            <th>Description</th>
                            <th>Thumbnail</th>
                            <th>Edit</th>
                            <th>Remove</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Service service in Model)
                        {
                            <tr>
                                <td>@service.Id</td>
                                <td>@service.Title</td>
                                <td>@service.Description</td>
                                <td><img style="width: 60px" src="~/img/@service.ThumbnailPath" alt="@service.Title" /></td>
                                <td><a class="btn btn-primary" asp-area="Admin" asp-controller="Service" asp-action="Edit" asp-route-id="@service.Id">Edit</a></td>
                                <td><a class="btn btn-danger" asp-area="Admin" asp-controller="Service" asp-action="Remove" asp-route-id="@service.Id">Remove</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- /.container-fluid -->
<!-- Page level plugins -->
<script src="~/Admin/vendor/datatables/jquery.dataTables.min.js" defer></script>
<script src="~/Admin/vendor/datatables/dataTables.bootstrap4.min.js" defer></script>

<!-- Page level custom scripts -->
<script src="~/Admin/js/demo/datatables-demo.js" defer></script>